import { useState } from "react";
import { beverageCategories, beverageGallery } from "../../data/mainCourse";
import { SectionTitle } from "./SectionTitle";

export function BeverageGallery() { const [active, setActive] = useState("Show All"); const visible = active === "Show All" ? beverageGallery : beverageGallery.filter((item) => item.category === active); return <section className="bg-white py-16 md:py-20"><div className="mx-auto max-w-[900px] px-6 md:px-8"><SectionTitle title="Discover Our Beverages" subtitle="The Watering Hole" /><div className="mt-7 flex flex-wrap justify-center gap-1.5">{beverageCategories.map((category) => <button key={category} onClick={() => setActive(category)} className={`border px-3 py-1.5 text-xs transition-colors ${active === category ? "border-[#c8a04b] bg-[#c8a04b] text-white" : "border-[#858b90] text-[#46515c] hover:border-[#c8a04b] hover:text-[#a27b2b]"}`}>{category}</button>)}</div><div className="mt-8 grid grid-cols-2 gap-3 md:grid-cols-4">{visible.map((item, index) => <div key={`${item.image}-${index}`} className="group aspect-[1.45] cursor-pointer overflow-hidden"><img src={item.image} alt={`${item.category} beverage`} className="h-full w-full object-cover transition-transform duration-500 group-hover:scale-110" /></div>)}</div></div></section>; }
