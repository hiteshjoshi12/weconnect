import { motion } from "framer-motion";
import { blogPosts } from "../../data/mainCourse";
import { SectionTitle } from "./SectionTitle";

export function BlogSection() { return <section className="bg-white py-16 md:py-20"><div className="mx-auto max-w-[900px] px-6 md:px-8"><SectionTitle title="F&B Blog" subtitle="Good Food....Good Life !" /><div className="mt-10 grid gap-8 md:grid-cols-2">{blogPosts.map((post, index) => <motion.article key={post.title} initial={{ opacity: 0, y: 18 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ duration: 0.5, delay: index * 0.1 }} className="grid grid-cols-[0.9fr_1.1fr] border border-[#ececea] bg-[#fafaf9]"><div className="aspect-square overflow-hidden"><img src={post.image} alt={post.title} className="h-full w-full object-cover transition-transform duration-500 hover:scale-105" /></div><div className="p-5"><h3 className="text-base font-semibold leading-6 text-[#2f3a46]">{post.title}</h3><div className="mt-4 flex flex-wrap gap-x-3 gap-y-1 text-[10px] text-[#80858a]"><span>{post.date}</span><span>{post.author}</span></div><p className="mt-4 text-xs leading-5 text-[#5e6268]">{post.description}</p></div></motion.article>)}</div></div></section>; }
