import { motion } from "framer-motion";
import { ArrowUpRight } from "lucide-react";
import { itUpdates } from "../../data/informationTechnology";

export function ITAnnouncements() { return <section className="bg-white py-20 md:py-28"><div className="mx-auto max-w-[1400px] px-6 xl:px-10"><div className="mb-10 flex flex-col justify-between gap-4 border-b border-gray-200 pb-6 md:flex-row md:items-end"><div><p className="text-xs font-bold uppercase tracking-[0.25em] text-[#A57C35]">Stay updated</p><h2 className="mt-4 text-[clamp(34px,4vw,52px)] font-semibold tracking-[-0.04em] text-[#222]">Latest Updates</h2></div><p className="max-w-[300px] text-sm leading-6 text-[#777]">News and reminders from the IT team.</p></div><div>{itUpdates.map((update, index) => <motion.article key={update.title} initial={{ opacity: 0, x: -18 }} whileInView={{ opacity: 1, x: 0 }} viewport={{ once: true }} transition={{ duration: 0.5, delay: index * 0.1 }} className="group grid gap-5 border-b border-gray-200 py-7 md:grid-cols-[100px_1fr_auto] md:items-center"><div><p className="text-3xl font-semibold leading-none text-[#A57C35]">{update.date}</p><p className="mt-2 text-xs font-bold tracking-[0.2em] text-gray-500">{update.month}</p></div><div><h3 className="text-xl font-semibold text-[#222] transition-colors group-hover:text-[#A57C35]">{update.title}</h3><p className="mt-2 text-sm leading-6 text-[#777]">{update.description}</p></div><ArrowUpRight size={20} className="text-[#C79A43] transition-transform group-hover:-translate-y-1 group-hover:translate-x-1" /></motion.article>)}</div></div></section>; }
