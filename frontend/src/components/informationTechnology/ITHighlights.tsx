import { motion } from "framer-motion";
import { itFocusAreas } from "../../data/informationTechnology";

export function ITHighlights() {
  return <section className="bg-[#0c1420] py-20 text-white md:py-28"><div className="mx-auto max-w-[1400px] px-6 xl:px-10"><motion.div initial={{ opacity: 0, y: 18 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ duration: 0.55 }}><p className="text-xs font-bold uppercase tracking-[0.25em] text-[#D8B96A]">Technology with purpose</p><h2 className="mt-5 max-w-[620px] text-[clamp(34px,4.5vw,58px)] font-semibold leading-[1.05] tracking-[-0.04em]">Technology with <span className="text-[#D8B96A]">purpose.</span></h2></motion.div><div className="mt-16 grid gap-0 border-y border-white/15 md:grid-cols-3">{itFocusAreas.map((area, index) => { const Icon = area.icon; return <motion.div key={area.title} initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ duration: 0.55, delay: index * 0.1 }} className="group border-b border-white/15 py-9 md:border-b-0 md:border-r md:px-8 md:first:pl-0 md:last:border-r-0"><div className="flex items-center justify-between"><span className="text-sm text-[#D8B96A]">{area.number}</span><Icon size={21} className="text-white/50 transition-colors group-hover:text-[#D8B96A]" /></div><h3 className="mt-12 text-2xl font-semibold capitalize">{area.title}</h3><p className="mt-4 max-w-[270px] text-sm leading-7 text-white/60">{area.description}</p></motion.div>; })}</div></div></section>;
}
